services:
  app:
    container_name: stk-soporte_app
    build:
      context: .
    env_file:
      - .env.production
    environment:
      DATA_SOURCE: postgres
      DATABASE_URL: postgresql://stockgi_app:${POSTGRES_APP_PASSWORD}@postgres:5432/stockgi_soporte_ti
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "127.0.0.1:${APP_PORT:-3002}:3001"
    volumes:
      - stk-soporte_storage_data:/var/lib/stockgi/uploads
    networks:
      - stk-soporte_internal
    restart: unless-stopped

  postgres:
    container_name: stk-soporte_postgres
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: stockgi_soporte_ti
      POSTGRES_USER: stockgi_migrator
      POSTGRES_PASSWORD: ${POSTGRES_MIGRATOR_PASSWORD}
      POSTGRES_APP_PASSWORD: ${POSTGRES_APP_PASSWORD}
    volumes:
      - stk-soporte_postgres_data:/var/lib/postgresql/data
      - ./database/init:/docker-entrypoint-initdb.d:ro
    networks:
      - stk-soporte_internal
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U stockgi_migrator -d stockgi_soporte_ti"]
      interval: 10s
      timeout: 5s
      retries: 10
    restart: unless-stopped

  cloudflared:
    container_name: stk-soporte_cloudflared
    image: cloudflare/cloudflared:latest
    command: tunnel --no-autoupdate run --token ${CLOUDFLARE_TUNNEL_TOKEN}
    depends_on:
      - app
    networks:
      - stk-soporte_internal
    restart: unless-stopped
    profiles:
      - tunnel

  backup:
    container_name: stk-soporte_backup
    image: postgres:16-alpine
    env_file:
      - .env.production
    volumes:
      - stk-soporte_backups:/backups
      - ./scripts:/scripts:ro
    networks:
      - stk-soporte_internal
    entrypoint: ["/bin/sh", "-c", "while true; do /scripts/backup-postgres.sh; sleep 86400; done"]
    profiles:
      - backup

networks:
  stk-soporte_internal:
    name: stk-soporte_net

volumes:
  stk-soporte_postgres_data:
    name: stk-soporte_postgres_data
  stk-soporte_storage_data:
    name: stk-soporte_storage_data
  stk-soporte_backups:
    name: stk-soporte_backups